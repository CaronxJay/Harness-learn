1. .env.example — 放模板（占位符），用户复制为 .env 后填入自己的 Key：
# .env.example
LLM_PROVIDER=deepseek          # deepseek / qwen / openai
DEEPSEEK_API_KEY=sk-your-key-here
# GITHUB_TOKEN=ghp_xxx         # 可选，GitHub API 限流高时用
BUDGET_YUAN=1.0                # 日预算上限
PLANNER_TARGET_COUNT=10        # 采集目标条目数
2. pip install -r requirements.txt 安装依赖即可跑。
